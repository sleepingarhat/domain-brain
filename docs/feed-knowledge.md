# 點饋文章／知識入天喜腦

饋完一定要 **compile + build**，查詢先會搞到原文同實體頁：

```bash
python -m brain.cli compile
python -m brain.cli build
python -m brain.cli query "你的問題"
```

---

## 方法 1：自動（已設定）

每日 GitHub Actions 會拉：

- tianxi 賽果／預測
- HKJC、Idol Horse、The Standard（免費公開源）

你唔使每次手動饋呢啲。
